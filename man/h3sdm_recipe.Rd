% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/h3sdm_recipe.R
\name{h3sdm_recipe}
\alias{h3sdm_recipe}
\title{Create a tidymodels recipe for H3-based SDMs}
\usage{
h3sdm_recipe(data, response_col = "presence")
}
\arguments{
\item{data}{An \code{sf} object, typically the output of \code{h3sdm_data()},
including species presence-absence or count data, H3 addresses, and
environmental predictors. The geometry must be of type \code{MULTIPOLYGON}.}

\item{response_col}{\code{character} Name of the column to use as the outcome
(response variable). Default \code{"presence"} for presence/absence models.
Use \code{"count"} when working with count data generated by
\code{h3sdm_count_from_records()}.}
}
\value{
A \code{tidymodels} recipe object (class \code{"h3sdm_recipe"}) ready for modeling.
}
\description{
Prepares an \code{sf} object with H3 hexagonal data for modeling with the
\code{tidymodels} ecosystem. Extracts centroid coordinates, assigns appropriate
roles to the variables automatically, and returns a ready-to-use recipe for
modeling species distributions.
}
\details{
This function prepares spatial H3 grid data for species distribution modeling:
\itemize{
\item Extracts centroid coordinates (\code{x} and \code{y}) from MULTIPOLYGON geometries.
\item Removes the geometry column to create a purely tabular dataset for tidymodels.
\item Assigns roles to columns:
\itemize{
\item \code{response_col} → \code{"outcome"} (target variable)
\item \code{h3_address} → \code{"id"} (used for joining predictions later)
\item \code{x} and \code{y} → \code{"spatial_predictor"}
}
\item All other columns are assigned \code{"predictor"} role.
}
}
\examples{
\dontrun{
# Presence/absence model (default)
sdm_recipe <- h3sdm_recipe(combined_data)

# Count-based model
sdm_recipe <- h3sdm_recipe(combined_data, response_col = "count")
}

}
