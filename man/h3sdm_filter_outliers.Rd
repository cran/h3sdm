% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/h3sdm_filter_outliers.R
\name{h3sdm_filter_outliers}
\alias{h3sdm_filter_outliers}
\title{Filter environmental outliers from presence records using Mahalanobis distance}
\usage{
h3sdm_filter_outliers(pa, vars_cov = NULL, threshold = 0.975)
}
\arguments{
\item{pa}{An \code{sf} object produced by \code{\link[=h3sdm_pa_from_records]{h3sdm_pa_from_records()}} or
\code{\link[=h3sdm_pa]{h3sdm_pa()}}, containing a \code{presence} column with values
\code{"1"} (presence) and \code{"0"} (pseudo-absence), plus numeric
covariate columns.}

\item{vars_cov}{Character vector of covariate column names to use for the
Mahalanobis distance calculation. Typically the bioclimatic
variables (e.g. CHELSA). If \code{NULL} (default), all numeric
columns except \code{h3_address}, \code{presence}, \code{x}, and \code{y} are
used automatically.}

\item{threshold}{Numeric in (0, 1). Percentile of the chi-squared
distribution used as the outlier threshold.
Default \code{0.975}.}
}
\value{
A list with four elements:
\describe{
\item{\code{pa_clean}}{\code{sf} object -- the input PA dataset with outlier
presences removed. Pseudo-absences are untouched.}
\item{\code{outliers_df}}{\code{data.frame} with the removed records and an
additional column \code{mahal_d2} with their D^2 values. Empty
\code{data.frame} if no outliers were detected.}
\item{\code{n_removed}}{Integer. Number of presence records removed.}
\item{\code{threshold_d2}}{Numeric. The D^2 threshold value used
(\code{qchisq(threshold, df = k)}).}
}
}
\description{
Identifies and removes presence records that are environmental outliers
based on their Mahalanobis distance (D^2) to the centroid of the presence
cloud in environmental space. Only presence records (\code{presence == "1"}) are
evaluated; pseudo-absences are always retained unchanged.
}
\details{
The threshold is derived from the chi-squared distribution:
\code{qchisq(threshold, df = length(vars_cov))}. A record is flagged as an
outlier when its D^2 exceeds this value. The default \code{threshold = 0.975}
corresponds to the standard 97.5th percentile used in SDM practice.

\strong{Why only presences?} Pseudo-absences are algorithmically generated by
\code{h3sdm} and carry no georeferencing or identification error. Filtering them
would remove points at the environmental periphery -- exactly those most
informative for defining the species niche boundary.

\strong{Singular covariance matrix:} When presences are too few or covariates are
perfectly collinear the covariance matrix becomes singular and cannot be
inverted. In that case the function returns the original PA unchanged with a
warning.

\strong{Relationship with AOA:} The AOA (see \code{\link[=h3sdm_aoa]{h3sdm_aoa()}}) evaluates prediction
reliability \emph{after} training. This function improves \emph{input data quality}
\emph{before} training. Both are complementary: an unfiltered outlier can
artificially expand the AOA into ecologically implausible areas.
}
\examples{
\dontrun{
# After generating the PA dataset:
pa <- h3sdm_pa_from_records(records, aoi_sf, res = 7L,
                             predictors_sf = cov_actual)

# Auto-detect covariates and filter with default threshold (97.5\%)
result <- h3sdm_filter_outliers(pa)

# Inspect removed records
result$outliers_df

# Use the clean PA for modeling
dat <- h3sdm_data(result$pa_clean, pred_sf)

# Custom covariate selection and stricter threshold
result2 <- h3sdm_filter_outliers(pa,
                                  vars_cov  = c("bio1", "bio12", "bio15"),
                                  threshold = 0.95)
}

}
\seealso{
\code{\link[=h3sdm_pa_from_records]{h3sdm_pa_from_records()}}, \code{\link[=h3sdm_aoa]{h3sdm_aoa()}}
}
