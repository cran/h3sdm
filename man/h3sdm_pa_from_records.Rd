% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/h3sdm_pa_from_records.R
\name{h3sdm_pa_from_records}
\alias{h3sdm_pa_from_records}
\title{Generate presence/pseudo-absence dataset from user-provided records}
\usage{
h3sdm_pa_from_records(
  records,
  aoi_sf,
  res = 6,
  n_pseudoabs = 500,
  expand_factor = 0.1,
  lon_col = "lon",
  lat_col = "lat",
  species_col = NULL,
  geospatial_filter = TRUE
)
}
\arguments{
\item{records}{\code{data.frame} or \code{sf} object containing presence records.}

\item{aoi_sf}{\code{sf} AOI (area of interest) polygon.}

\item{res}{\code{integer} H3 resolution for the hexagonal grid.}

\item{n_pseudoabs}{\code{integer} Number of pseudo-absence hexagons to sample.}

\item{expand_factor}{\code{numeric} Factor to expand AOI before creating hex grid.}

\item{lon_col}{\code{character} Name of the longitude column. Ignored if \code{records}
is already an \code{sf} object.}

\item{lat_col}{\code{character} Name of the latitude column. Ignored if \code{records}
is already an \code{sf} object.}

\item{species_col}{\code{character} Optional. Name of the column containing the species
name. If provided, the column is retained in the output as metadata.}

\item{geospatial_filter}{\code{logical} If \code{TRUE} (default) and the input contains
a \code{geospatialKosher} column, records with \code{geospatialKosher == FALSE} are
removed before processing. Ignored if the column is absent.}
}
\value{
\code{sf} object with columns:
\describe{
\item{h3_address}{H3 index of the hexagon.}
\item{presence}{Factor with levels \code{"0"} (pseudo-absence) and \code{"1"} (presence).}
\item{species}{Species name (only if \code{species_col} is provided).}
\item{geometry}{MULTIPOLYGON of each hexagon.}
}
}
\description{
Adapts a user-provided dataset with presence records (from personal fieldwork,
BiodataCR, or any other source) into a hexagonal presence/pseudo-absence dataset
ready for analysis with h3sdm. The input can be a \code{data.frame} with coordinate
columns or an \code{sf} object. Coordinates are assumed to be in WGS84 (EPSG:4326).
}
\examples{
\donttest{
data(cr_outline_c, package = "h3sdm")

my_records <- data.frame(
  lon = c(-84.1, -84.2, -83.9),
  lat = c(9.9, 10.1, 9.8),
  species = "Agalychnis callidryas"
)

dataset <- h3sdm_pa_from_records(
  records     = my_records,
  aoi_sf      = cr_outline_c,
  res         = 7,
  n_pseudoabs = 100,
  lon_col     = "lon",
  lat_col     = "lat",
  species_col = "species"
)
}
}
