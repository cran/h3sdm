% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/h3sdm_pres_from_sf.R
\name{h3sdm_pres_from_sf}
\alias{h3sdm_pres_from_sf}
\title{Assign pre-downloaded species presence records to H3 hexagons}
\usage{
h3sdm_pres_from_sf(records_sf, aoi_sf, res = 6, expand_factor = 0.1)
}
\arguments{
\item{records_sf}{An \code{sf} object with presence records in any CRS.
Typically the output of \code{\link[=h3sdm_get_records]{h3sdm_get_records()}}.}

\item{aoi_sf}{An \code{sf} object defining the area of interest.}

\item{res}{Integer. H3 resolution (0–15). Default is \code{6}.}

\item{expand_factor}{Numeric. Expansion factor for the H3 grid beyond the
AOI bounding box. Default is \code{0.1}.}
}
\value{
An \code{sf} object with one row per presence hexagon, containing:
\describe{
\item{h3_address}{H3 index of the hexagon.}
\item{n}{Number of records assigned to the hexagon.}
\item{geometry}{MULTIPOLYGON geometry of the hexagon.}
}
}
\description{
Takes an \code{sf} object of species occurrence records already downloaded
(e.g. from \code{\link[=h3sdm_get_records]{h3sdm_get_records()}}) and assigns them to H3 hexagons,
returning only hexagons with at least one presence record.

This function extracts the hexagon-assignment logic from
\code{\link[=h3sdm_get_records_by_hexagon]{h3sdm_get_records_by_hexagon()}} without downloading records internally,
making it suitable for workflows where records have already been retrieved.
}
\details{
This function is designed to be used in combination with
\code{\link[=h3sdm_filter_outliers]{h3sdm_filter_outliers()}} and \code{\link[=h3sdm_pa]{h3sdm_pa()}} for a balanced
presence/pseudo-absence workflow:

\preformatted{
# 1. Download records
records_sf <- h3sdm_get_records("Species name", aoi_sf, providers = c("gbif", "biodatacr"))

# 2. Assign to hexagons
pres_sf <- h3sdm_pres_from_sf(records_sf, aoi_sf, res = 7)

# 3. Filter environmental outliers (only presences)
filtro <- h3sdm_filter_outliers(pres_sf_env, vars_cov)
pres_clean <- filtro$pa_clean

# 4. Generate balanced pseudo-absences (1:1)
pa <- h3sdm_pa(pres_clean, predictors_sf, n_pseudoabs = nrow(pres_clean))
}
}
\examples{
\dontrun{
data(cr_outline_c, package = "h3sdm")

records_sf <- h3sdm_get_records(
  species   = "Panthera onca",
  aoi_sf    = cr_outline_c,
  providers = c("gbif", "biodatacr"),
  limit     = 500
)

pres_sf <- h3sdm_pres_from_sf(records_sf, cr_outline_c, res = 7)
nrow(pres_sf)
}

}
\seealso{
\code{\link[=h3sdm_get_records]{h3sdm_get_records()}}, \code{\link[=h3sdm_pa]{h3sdm_pa()}}, \code{\link[=h3sdm_filter_outliers]{h3sdm_filter_outliers()}}
}
